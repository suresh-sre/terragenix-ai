{
  "name": "genopslab-root",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n backend,frontend \"python backend/index.py\" \"npm run dev --prefix frontend\"",
    "dev:backend": "python backend/index.py",
    "dev:frontend": "npm run dev --prefix frontend",
    "build:frontend": "npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
